<UserControl x:Class="Ironwall.Libraries.Dotnet.Ollama.Ui.Views.ChatBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
			 xmlns:behaviors="clr-namespace:Ironwall.Libraries.Dotnet.Ollama.Ui.Behaviors"
			 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
			 xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
			 xmlns:cal="http://caliburnmicro.com"
             xmlns:local="clr-namespace:Ironwall.Libraries.Dotnet.Ollama.Ui.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid Margin="5">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="50"/>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="50"/>
			<ColumnDefinition Width="50"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="30"/>
		</Grid.RowDefinitions>
		<RichTextBox Grid.Column="0" Grid.ColumnSpan="4"
				 Foreground="WhiteSmoke"
				 VerticalScrollBarVisibility="Visible"
				 HorizontalScrollBarVisibility="Auto"
				 behaviors:RichTextBoxBehavior.BindableDocument="{Binding RichTextContent}" 
				 behaviors:RichTextBoxScrollBehavior.AutoScroll="True">

		</RichTextBox>
		<mah:ProgressRing Grid.Row="1" Grid.Column="0" 
					  Foreground="WhiteSmoke" 
					  Width="15" Height="15"  
					  Visibility="{Binding Visibility, Converter={StaticResource BooleanToVisibilityConverter}}"
					  VerticalAlignment="Center" HorizontalAlignment="Center"/>
		<TextBox Grid.Row="1" Grid.Column="1"
			 Margin="0 2 2 0 "
			 Background="#88000000"
			 Foreground="WhiteSmoke"
			 FontSize="12"
			 VerticalContentAlignment="Center"
			 IsEnabled="{Binding IsReady}"
			 cal:Message.Attach = "[Event KeyDown]=[Action OnTextBoxKeyDown($eventArgs)];"
			 Text="{Binding InputTextContent, UpdateSourceTrigger=PropertyChanged, FallbackValue=Text input this line}"
			 />
		<Button x:Name="InputButton"
			Margin="0 2 2 0 "
			IsEnabled="{Binding IsReady}"
			Grid.Row="1" Grid.Column="2">
			<Button.Content>
				<iconPacks:RemixIcon Kind="SendPlane2Fill" Height="15" Width="15"/>
			</Button.Content>
		</Button>

		<Button x:Name="CreateModel"
				Margin="0 2 0 0 "
				IsEnabled="{Binding IsReady}"
				Grid.Row="1" Grid.Column="3">
			<Button.Content>
				<iconPacks:RemixIcon Kind="Save2Fill" Height="15" Width="15"/>
			</Button.Content>
		</Button>
	</Grid>
</UserControl>
